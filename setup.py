from setuptools import setup

setup(
    name='ia_detector',
    author='Thedmer Postma',
    version='0.0.1',
    install_requires=[
        'monthdelta',
        'earthengine-api',
        'folium',
    ]
)